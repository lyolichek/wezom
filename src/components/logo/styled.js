import styled from 'styled-components';

export const StyledLogo = styled.div`
    position: relative;
    width: 5.7rem;
    margin: 0;
    margin: 0 1.8rem;
    
    .logo__img {
        max-width: 100%;
    }

    .is-active {   
        pointer-events: none;
    }
`;
