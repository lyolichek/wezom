import styled from 'styled-components';

export const StyledFilter = styled.div`
    margin: 0 0 2rem;
    display: flex;
    
    input {
        max-width: 50rem;
        margin: 0 1.6rem 0 0;
    }
    
    select {
        max-width: 25rem;
        margin: 0 1.6rem 0 0;
    }
`;
