import styled from 'styled-components';

export const StyledTitle = styled.h1`
    margin: 0 0 2rem;
    color: rgba(0,0,0,.85);
    font-weight: 600;
    font-size: 3.2rem;
    line-height: 1.2;
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
`;
